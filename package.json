{
  "name": "montecristiao-public",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir .build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8774"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  },
  "dependencies": {
    "better-auth": "1.7.3",
    "jose": "6.2.12"
  }
}
